create table T (id i, name c20) distribute name ("Adi", "Hem");

create index T (id);

load T ("../data/t.data");

set partitionedPrint = "TRUE";
set bQueryPlans = "1";

print T;

Update T Set name = "Baby" Where name >= "Hem";

print T;
